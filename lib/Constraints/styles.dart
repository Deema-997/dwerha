import 'package:flutter/material.dart';
import 'colors.dart';

const logoStyle = TextStyle(
    color: Color(text_color), fontSize: 30.0, fontWeight: FontWeight.bold);

const smallStyle = TextStyle(
  color: Color(text_color),
  fontSize: 14.0,
);

const smallStyleWhite = TextStyle(
  color: Colors.white,
  fontSize: 14.0,
);

const normalStyle = TextStyle(
  color: Color(text_color),
  fontSize: 16.0,
);
